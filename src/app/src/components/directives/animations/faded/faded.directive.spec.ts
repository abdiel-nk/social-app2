import { FadedDirective } from './faded.directive';

describe('FadedDirective', () => {
  it('should create an instance', () => {
    const directive = new FadedDirective();
    expect(directive).toBeTruthy();
  });
});
